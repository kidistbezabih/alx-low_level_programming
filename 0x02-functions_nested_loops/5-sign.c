#include "main.h"

/**
 * _isalpha - the function
 *@c: parameter
 *If :return 1.
 * Return: Always 0.
 */

int _isign(int c)
{
if ((c >= 65 && c <= 90) || (c >= 97 && c <= 122))
{
return (1);
}
return (0);
}
