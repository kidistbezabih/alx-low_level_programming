#include "main.h"

/**
*peint_rev-function
*@s: char argument
*Return: void
*/

void print_rev(char *s)
{
int len;
int i;
len = _strlen(char *s);

for(i = len; i > 0; i--)
{
_putchar(str[i]);
}
_putchar('\n');
}
